import { publications } from '../data.js'
export default function Publications(){return <section id="publications" className="section"><SectionHead icon="▤" title="Publications & Presentations" text="Research and knowledge sharing through publications and conferences."/><div className="publication-grid">{publications.map(p=><article className={'publication-card '+(!p.url?'presentation':'')} key={p.title}><div className="pub-icon"><span>▤</span></div><div className="pub-body"><div className="pub-meta"><span>{p.venue}</span><b>{p.type}</b></div><h3>{p.title}</h3><p>{p.description}</p>{p.url&&<a href={p.url} target="_blank" rel="noreferrer">View Publication <span>→</span></a>}</div></article>)}</div></section>}
function SectionHead({icon,title,text}){return <div className="section-head"><div className="section-icon">{icon}</div><div><h2>{title}</h2><p>{text}</p></div></div>}
